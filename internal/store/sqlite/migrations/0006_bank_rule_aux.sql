-- ---------------------------------------------------------------------------
-- 匹配规则支持完整的四维辅助核算
--
-- 原表只有 contact_id，因为设计时把「往来单位」当成了规则唯一要填的维度。
-- 实际用起来立刻撞墙：一条「房租 → 管理费用—租赁费」的规则永远过不了账，
-- 因为 560210 要求「部门」辅助核算，而规则没法表达它 ——
-- 匹配成功、生成凭证时被拦下，用户看着「规则命中 2 条」却一条也记不上。
--
-- 四维（往来/员工/部门/项目）是本工程从一开始就定下的辅助核算模型，
-- 规则作为「自动记账方案」的载体必须能完整表达它。
-- ---------------------------------------------------------------------------

ALTER TABLE bank_rule ADD COLUMN employee_id INTEGER;
ALTER TABLE bank_rule ADD COLUMN dept_id     INTEGER;
ALTER TABLE bank_rule ADD COLUMN project_id  INTEGER;
